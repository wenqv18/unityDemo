using UnityEngine;

/// <summary>
/// Temporary entry point for the backpack button. Replace the log with real backpack UI logic later.
/// </summary>
public sealed class BackpackButtonController : MonoBehaviour
{
    public void OpenBackpack()
    {
        Debug.Log("打开背包");
    }
}
